# Fixtures whose frames trip the structural lint today. One stem per line,
# with its cause. Empty since ADR-402: every line end a fixture draws is
# closed by an arm, an arrowhead fed from its tail, or text.
#
# A regression belongs here with the cause that put it here, and leaves when
# the cause is gone. Fix the renderer rather than lengthening the list.
