# Binary
/mmaid
# Scratch diagram files
/*.mmd
termaid
# makepkg build artifacts
/pkg/
/src/
*.pkg.tar.zst
# Release source archive (PKGBUILD downloads vX.Y.Z.tar.gz)
/*.tar.gz
# Cross-compiled binaries
/dist/
# make package dry run
/pkgbuild-check/
